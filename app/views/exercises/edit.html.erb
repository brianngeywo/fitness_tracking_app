<h1>Editing <%= @workout.name %> Exercise</h1>

<%= render 'form', exercise: @exercise %>

<%= link_to 'Show', workout_exercise_path(@exercise) %> |
<%= link_to 'Back', workout_exercises_path(@workout) %>
